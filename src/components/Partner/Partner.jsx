export const Partner = ({ partner }) => (
  <div>
    <section className="Partner">
      <p className="Person__partner">Is my partner {partner}</p>
    </section>
  </div>
);

export default Partner;
